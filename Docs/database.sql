-- Nếu không muốn EF EnsureCreated, bạn có thể tạo DB trước bằng script này.
CREATE DATABASE IF NOT EXISTS ql_banhang CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE ql_banhang;

-- Bảng schema do EF tạo sẽ tương tự. Script này chỉ để tham khảo/backup.
